@page "/auth/login"

@layout AuthLayout
@inject NavigationManager navigationManager
<div class="row">
    <div class="col-12">
        <h2>Login to your account</h2>
        <RadzenCard>
            <label>Email</label>
            <RadzenTextBox></RadzenTextBox>
            <label>Password</label>
            <RadzenPassword></RadzenPassword>
            <hr />
            <RadzenButton ButtonType="Radzen.ButtonType.Button" Text="Login"
                          ButtonStyle="Radzen.ButtonStyle.Primary" class="btn-block mr-2" Click="GoToHome" />
        </RadzenCard>
    </div>
</div>

@code{
    void GoToHome()
    {
        //Save local user
        //var userInfo = new PlannerApp.Client.Models.LocalUserInfo()
        //{
        //  Email = result.UserInfo["Email"],
        //};
        //await storageService.SetItemAsync("User", userInfo);
        //await authenticationStateProvider.GetAuthenticationStateAsync();
        navigationManager.NavigateTo("/");
    }
}
